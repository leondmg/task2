<h1>Sign In</h1>

<%= form_with(url: session_path, local: true) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name, autofocus: true %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "Sign In" %>

<% end %>
